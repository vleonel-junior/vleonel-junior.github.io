---
interface Props {
    title: string;
    id?: string;
}

const { title, id } = Astro.props;
---

<h2 id={id} class="mb-6 text-sm font-semibold uppercase tracking-wider text-fg-subtle">
    {title}
</h2>
